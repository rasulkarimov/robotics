# Python deps for the xArm control venv (/home/astra/tools/venv).
# Setup: python3 -m venv /home/astra/tools/venv && /home/astra/tools/venv/bin/pip install -r requirements.txt
# See arm.py module docstring for why this lives in a venv (system pip is PEP 668 blocked).
hidapi==0.15.0
xarm==0.0.4
